---

- name: Configure instance
  hosts: localhost
  connection: local
  vars_files:
    - "environments/{{ stack_name }}.yml"
    - my_vars.yml
  roles:
    - ses
    - mysql
    - memcached
    - customs
    - authdb
    - auth
    - content
    - log
    - cron_update
    - team

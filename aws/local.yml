---

- name: Configure instance
  hosts: localhost
  connection: local
  vars_files:
    - "defaults.yml"
    - "environments/{{ stack_name }}.yml"
  roles:
    - common
    - mysql
    - ses
    - redis
    - memcached
    - customs
    - authdb
    - auth
    - content
    - basket-proxy
    - rp-build
    - rp
    - rp-untrusted
    - oauth
    - oauth-console
    - verifier
    - sync
    - profile
    - docker-gc
    - cron_update
    - team
